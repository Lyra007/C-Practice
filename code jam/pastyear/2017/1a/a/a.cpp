#include <iostream>
#include <queue>
#include  
#include <vector>
using namespace std;

class cmp {
public:
  bool operator() (const 
};

typedef priority_queue<int, vector<int>, cmp>;

int main() {
  freopen("A-small-practice.in", "r" stdin);
  int num_cases;
  cin >> num_cases;

  for(int i=0; i<num_cases; i++) {
    int K, N;
    int max_r, max_h;
    cin >> k >> N;
    int tmp = K;
    while(tmp) {
      
      tmp--;
    }
    
  }
  cout << "hello world" <<endl;
  return 0;
  
}
